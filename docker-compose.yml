name: federation-golang
services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "9090:8080"
    depends_on:
      db:
        condition: service_healthy
    command:
      - "/federation-backend"
    volumes:
      - files:/app/files
    restart: always

volumes:
  files:
